interface:
  display_name: "Album Conceptualizer"
  short_description: "Designs album concepts, tracklist architecture, and..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:album-conceptualizer for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
