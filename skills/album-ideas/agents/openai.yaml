interface:
  display_name: "Album Ideas"
  short_description: "Tracks and manages album ideas including brainstorming,..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:album-ideas for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
