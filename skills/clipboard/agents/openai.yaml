interface:
  display_name: "Clipboard"
  short_description: "Copies track content (lyrics, style prompts, streaming..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:clipboard for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
