interface:
  display_name: "Document Hunter"
  short_description: "Searches and retrieves documents from free public sources..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:document-hunter for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
