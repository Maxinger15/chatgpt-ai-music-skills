interface:
  display_name: "Explicit Checker"
  short_description: "Scans lyrics for explicit content and verifies that..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:explicit-checker for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
