interface:
  display_name: "Genre Creator"
  short_description: "Create new genre documentation files for the maxinger15-music..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:genre-creator for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
