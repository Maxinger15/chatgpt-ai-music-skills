interface:
  display_name: "Import Track"
  short_description: "Moves track markdown files to the correct album location...."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:import-track for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
