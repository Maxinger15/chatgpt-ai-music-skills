interface:
  display_name: "Lyric Reviewer"
  short_description: "Reviews lyrics against a quality checklist before Suno..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:lyric-reviewer for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
