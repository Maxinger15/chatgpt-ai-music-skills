interface:
  display_name: "Lyric Writer"
  short_description: "Writes or reviews lyrics with professional prosody, rhyme..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:lyric-writer for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
