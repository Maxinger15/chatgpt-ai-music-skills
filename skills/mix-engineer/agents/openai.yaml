interface:
  display_name: "Mix Engineer"
  short_description: "Polishes raw Suno audio by processing per-stem WAVs..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:mix-engineer for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
