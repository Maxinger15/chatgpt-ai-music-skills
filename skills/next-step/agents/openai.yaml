interface:
  display_name: "Next Step"
  short_description: "Analyzes album state and recommends the optimal next..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:next-step for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
