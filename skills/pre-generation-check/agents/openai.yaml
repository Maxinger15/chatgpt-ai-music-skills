interface:
  display_name: "Pre Generation Check"
  short_description: "Validates all pre-generation gates before sending tracks to..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:pre-generation-check for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
