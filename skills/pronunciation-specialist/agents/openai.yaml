interface:
  display_name: "Pronunciation Specialist"
  short_description: "Scans lyrics for pronunciation risks and prevents Suno..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:pronunciation-specialist for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
