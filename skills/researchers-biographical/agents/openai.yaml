interface:
  display_name: "Researchers Biographical"
  short_description: "Researches personal backgrounds, interviews, motivations,..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:researchers-biographical for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: false
