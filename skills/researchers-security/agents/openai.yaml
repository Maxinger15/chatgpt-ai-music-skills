interface:
  display_name: "Researchers Security"
  short_description: "Researches malware analysis, CVEs, attribution reports, and..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:researchers-security for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: false
