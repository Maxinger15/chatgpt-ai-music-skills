interface:
  display_name: "Resume"
  short_description: "Finds an album by name and shows detailed status with next..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:resume for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
