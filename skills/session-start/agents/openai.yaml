interface:
  display_name: "Session Start"
  short_description: "Runs the session startup procedure - verifies setup, loads..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:session-start for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
