interface:
  display_name: "Sheet Music Publisher"
  short_description: "Converts mastered audio to sheet music and creates..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:sheet-music-publisher for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
