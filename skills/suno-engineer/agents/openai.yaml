interface:
  display_name: "Suno Engineer"
  short_description: "Constructs technical Suno V5/V5.5 style prompts, selects..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:suno-engineer for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
