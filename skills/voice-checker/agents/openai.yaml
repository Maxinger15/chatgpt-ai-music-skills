interface:
  display_name: "Voice Checker"
  short_description: "Reviews lyrics and prose for AI-written patterns (abstract..."
  brand_color: "#2563EB"
  default_prompt: "Use $maxinger15-music:voice-checker for this music workflow task."

dependencies:
  tools:
    - type: "mcp"
      value: "maxinger15-music-mcp"
      description: "Structured album, track, config, and processing tools for maxinger15 Music."
      transport: "stdio"

policy:
  allow_implicit_invocation: true
